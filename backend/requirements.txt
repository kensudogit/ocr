# ── Web フレームワーク ──────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
aiofiles==23.2.1

# ── データベース ────────────────────────────────────────────
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# ── 設定管理 ────────────────────────────────────────────────
pydantic-settings==2.6.1
python-dotenv==1.0.1

# ── 画像処理 ────────────────────────────────────────────────
opencv-python-headless==4.10.0.84
Pillow==11.0.0
numpy==2.1.3
scikit-image==0.24.0

# ── OCR エンジン ─────────────────────────────────────────────
paddlepaddle==2.6.2
paddleocr==2.9.1
# Google Vision API (オプション: .env で GOOGLE_VISION_API_KEY 設定時に使用)
google-cloud-vision==3.8.1

# ── データ抽出・自然言語処理 ──────────────────────────────────
python-dateutil==2.9.0
jaconv==0.3.4          # 日本語全半角変換
neologdn==0.5.3        # 日本語テキスト正規化
mojimoji==0.0.13       # 全角半角変換

# ── PDF 対応 ─────────────────────────────────────────────────
pdf2image==1.17.0
pdfplumber==0.11.4
pymupdf==1.24.14

# ── バックグラウンドタスク ────────────────────────────────────
celery==5.4.0
redis==5.2.1

# ── VLM（Vision Language Model）──────────────────────────────────────
# OpenAI GPT-4o（OPENAI_API_KEY 設定時に使用）
openai==1.57.0
# Google Gemini（GEMINI_API_KEY 設定時に使用）
google-generativeai==0.8.3

# ── freee 公式 API 連携 ────────────────────────────────────────────────
httpx==0.28.1       # 非同期 HTTP クライアント

# ── AWS Bedrock（国内リージョン AI 処理）────────────────────────────
# AI_DEPLOYMENT_MODE=bedrock 時に使用
boto3==1.35.90

# ── Google Vertex AI（国内リージョン AI 処理）─────────────────────────
# AI_DEPLOYMENT_MODE=vertex 時に使用
# google-cloud-aiplatform==1.74.0  # 必要時にコメントアウトを解除

# ── 暗号化（ファイル保存時 AES-256）────────────────────────────────
cryptography==44.0.0

# ── テスト ──────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-html==4.1.1          # HTML レポート生成
pytest-cov==6.0.0           # カバレッジ計測
coverage==7.6.9             # coverage.py 本体
aiosqlite==0.20.0           # テスト用インメモリ SQLite（非同期）
anyio==4.7.0                # pytest-asyncio 依存
